# Crypto Trading MVP - Streamlit Secrets
# Configurações sensíveis (serão sobrescritas por variáveis de ambiente)

[database]
url = "postgresql://trading:trading123@postgres:5432/trading_db"

[api]
url = "http://trading-api:8000"
timeout = 30

[auth]
jwt_secret = "your-jwt-secret-key"
session_timeout = 3600

[trading]
demo_mode = true
max_positions = 3
risk_per_trade = 0.02
